【背景】
我需要有一个可以用于 录音转 文字的 可以在本地识别的功能。
【构想】
我打算用Whisper 作为底座模型。
我打算程序布置在本地，然后我可以通过网页的形式，对话使用 录音转文字。
因为我是纯本地的，我也不需要账号密码什么的，但是网页要提供给我一些输入信息。比如 录音文件的地址。输出txt文件的地址（默认是跟录音文件地址一样），txt名称是 录音文件原名称+录音转文字。
界面做的现代一点。
我可以在页面里选不同的 Whisper 模型，不过我只需要大模型，因此只需要large-v3 和medium 两个模型就可以。
我不希望用docker 运行，因为都是本地运行，而whisper 可以用 命令行来运行，所以其实就是一个网页就解决了。网页是 用来交互友好，并执行 命令行的。 